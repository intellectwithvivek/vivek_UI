---
'@the_viveksingh/vivek-ui': patch
---

Add a `funding` field, so npm shows a "Fund this package" link and `npm fund` prints it.
The library stays MIT with no paid tier; this is a thank-you, not a gate.
